spring:
  application:
    name: demo  # 应用名称

  datasource:
    url: jdbc:mysql://localhost:3306/shield  # 数据库连接 URL
    username: root  # 数据库用户名
    password: qwer  # 数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver  # JDBC 驱动类名
